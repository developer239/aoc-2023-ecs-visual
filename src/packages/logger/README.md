# Automation Engine - Logger

Utility class for logging messages to output stream.

## Example

```cpp
int main() {
  Logger::Log("This is an info message.");
  Logger::Warn("This is an warning message.");
  Logger::Err("This is an error message.");
}
```
